  <% if target.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(target.errors.count, "error") %> prohibited this recaord from being saved:</h2>

     <!--
      <ul>
      <% target.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    -->
      <ul>
        <% target.errors.each_with_index do |msg, i| %>
        <li><%= msg[1] %></li>
        <% end %>
      </ul>
      
    </div>
  <% end %>
